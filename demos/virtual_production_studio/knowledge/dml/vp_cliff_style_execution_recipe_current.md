---
project: vp-studio-01
memory_class: tool_cookbook_event
phase: all
outcome: SUCCESS_VALIDATED
source: operator comparison and 2026-07-14 controller trajectory
validation: Use Cliff House phase flow with run_python(script=...) or run_csharp(script=...); no mutation or DML-call quotas; checkpoint with save_doc; use direct metadata-bearing 3dm Blender handoff
---

# VP Studio agentic execution recipe

VP execution recipe: use the working Cliff House generic phase pattern with the
VP brief, keep the existing Rhino slot, author bounded groups through
`run_python(script=...)` or `run_csharp(script=...)`, inspect at meaningful design
checkpoints, impose no mutation or DML-call quotas, save through `save_doc`, and
use the direct metadata-bearing `.3dm` Blender handoff. Keep Daystrom DML as
automatic continuity and retrieve prior decisions once at a meaningful phase
boundary; do not insert memory calls between ordinary geometry operations.

For Rhino, use the existing launcher-owned slot and the datum template. Author
bounded coherent geometry directly with `mcp_rhino_run_python(script=...)` or
`mcp_rhino_run_csharp(script=...)`. Continue naturally through several related
operations, then inspect objects and viewport at a meaningful design checkpoint.
Save timestamped checkpoints with `mcp_rhino_save_doc` when useful.

Avoid arbitrary mutation quotas, fixed-count DML checkpoints, interactive Rhino
New/Save/Export commands, duplicate slots, document close/reopen recovery, and
unrelated browser or shell fallbacks. At a real failure, inspect actual
application state, retrieve the prior failure pattern, and change the approach.

For Blender, use the metadata-preserving direct `.3dm` handoff and the shared
importer. Preserve all joined render-mesh parts, metadata, units, and bounding-box
parity before visual work.
