Continue the real Cliff House production workload from session 20260726_160512_e0b2e6 and the saved 41-object Rhino detailing checkpoint. The operator explicitly said to keep working. This is a second authorized bounded production turn.

This is a legacy continuation prompt. Before accepting its 41-object checkpoint, read deployment/aec-cptx-profile/canonical-cliff-house-geometry.txt and validate the checkpoint against every exact source-plan and massing bound plus the accepted HERO silhouette. Object count is not geometry proof. If geometry_parity.json or hero_geometry_validation.json does not pass, do not continue, render, or promote the checkpoint; preserve it only as rejected evidence and require a fresh canonical build.

Only if those geometry gates pass, verify that the connected Rhino slot still contains the 41 objects saved at aa_demo_versions/cliff_house_02/rhino_assets/base_model.3dm and that the ten documented detailing objects are present. If live and saved state differ, report the mismatch without closing or replacing the active document. Complete the remaining canonical Rhino detailing, independently validate it, save base_model.3dm, and create the next numbered backup and detailing checkpoint.

Continue sequentially through the canonical current-phase prompts: Rhino export, Blender import, lighting/camera, materials, test beauty/depth/segmentation renders, final rendering, and the checked-in scripts/comfyui_flux2_direct.py direct FLUX.2 workflow. Do not run SDXL or a depth ControlNet. Read only the current phase requirements as each phase begins. Review gates are automatically approved only after their validation checklists pass. OBS remains explicitly out of scope.

Before every Rhino geometry mutation, query DML exactly: Have I made successful Rhino geometry tool calls before? How exactly did I do that? Return the exact tool name, argument shape, validated script scaffold, and verified result. Exclude failed attempts. Reuse only the validated tool-call envelope; never reuse recalled geometry that conflicts with the canonical contract. Use C# run_csharp only for Rhino mutation, one visible object per call, with the required internal 0.2 second pause and independent name/layer/bounds verification. Store any new successful patterns in DML.

Before every substantive Rhino or Blender change, preserve the required numbered backup. Use the already-connected Rhino and Blender slots; do not launch or close either application. Save and validate artifacts before every phase transition. Use the checked-in scripts and project brief defaults; do not substitute the hero quick-demo asset and do not invent completion.

Prioritize completing a verifiable end-to-end still-image pipeline if the full 193-frame animation cannot fit in this turn: finished Rhino model, canonical Blender scene, one test beauty frame, one valid depth map, one valid segmentation mask, successful ComfyUI dry-run, and at least one accepted direct FLUX.2 processed frame. Then proceed to the full frame sequence if budget remains. Completion requires an artifact manifest with paths, counts, sizes, and pass/fail evidence.
